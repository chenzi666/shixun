<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.glw.mapper.FavoriteMapper">

    <!-- 添加收藏 -->
    <insert id="insert" parameterType="org.example.glw.entity.Favorite" useGeneratedKeys="true" keyProperty="favoriteId">
        INSERT INTO favorites (user_id, product_id, product_type, create_time)
        VALUES (#{userId}, #{productId}, #{productType}, #{createTime})
    </insert>

    <!-- 取消收藏 -->
    <delete id="deleteById">
        DELETE FROM favorites
        WHERE favorite_id = #{favoriteId} AND user_id = #{userId}
    </delete>

    <!-- 获取收藏列表 -->
    <select id="selectByUser" resultType="org.example.glw.entity.Favorite">
        SELECT favorite_id, user_id, product_id, product_type, create_time
        FROM favorites
        WHERE user_id = #{userId}
        <if test="type != null and type != ''">
            AND product_type = #{type}
        </if>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{size}
    </select>

    <!-- 统计收藏数量 -->
    <select id="countByUser" resultType="java.lang.Long">
        SELECT COUNT(*) 
        FROM favorites
        WHERE user_id = #{userId}
        <if test="type != null and type != ''">
            AND product_type = #{type}
        </if>
    </select>

    <!-- 检查是否已收藏 -->
    <select id="selectByUserAndProduct" resultType="org.example.glw.entity.Favorite">
        SELECT favorite_id, user_id, product_id, product_type, create_time
        FROM favorites
        WHERE user_id = #{userId} AND product_id = #{productId} AND product_type = #{productType}
    </select>

</mapper>