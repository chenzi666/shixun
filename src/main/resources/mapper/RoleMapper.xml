<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.glw.mapper.RoleMapper">

    <!-- 获取角色列表 -->
    <select id="selectRoles" resultType="org.example.glw.entity.Role">
        SELECT role_id, role_name, description, create_time
        FROM role
        ORDER BY role_id ASC
    </select>

    <!-- 根据ID获取角色 -->
    <select id="selectById" resultType="org.example.glw.entity.Role">
        SELECT role_id, role_name, description, create_time
        FROM role
        WHERE role_id = #{roleId}
    </select>

</mapper>